# Host port for dmarc-api (default: 8080)
# DMARC_API_PORT=8080

# Host port for dmarc-frontend (default: 3000)
# DMARC_FRONTEND_PORT=3000

# Path to directory containing DMARC reports (.gz and .zip). In Docker this is usually /data/DMARC.
# DMARC_DIR=./DMARC

# Known mail server IPs or CIDR ranges (e.g. Mailgun, SendGrid), comma-separated. 
# For example: 69.72.42.244,69.72.43.15,143.55.232.0/24
# KNOWN_SERVER_IPS=
